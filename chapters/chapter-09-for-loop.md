# 第9章 for 循环——重复的事情交给机器

> **难度**：⭐⭐⭐  
> **所属教程**：Python 零基础入门到 MicroPython 嵌入式实战  
> **前置章节**：第8章（列表）

---

### 9.1 for 循环基础

```python
# 打印 0 到 4
for i in range(5):
    print(i)

# 输出：
# 0
# 1
# 2
# 3
# 4
```

`range(5)` 生成 0, 1, 2, 3, 4 一共 5 个数字。**和索引一样，从 0 开始，不包括 5**。

### 9.2 range() 的几种用法

`range()` 有三种参数形式：

```python
range(5)          # 0, 1, 2, 3, 4           — 只有终点
range(1, 5)       # 1, 2, 3, 4              — 起点, 终点
range(1, 10, 2)   # 1, 3, 5, 7, 9           — 起点, 终点, 步长
```

和列表切片一样，`range()` 也是**起点包含，终点不包含**。第三个参数是**步长**。

#### 步长为负数——倒着数

步长不仅可以正，还可以负。负步长会**从大到小**生成数字：

```python
for i in range(5, 0, -1):   # 从 5 倒数到 1
    print(i)
# 输出：5 4 3 2 1

for i in range(10, 0, -2):  # 从 10 倒数到 1，每次减 2
    print(i)
# 输出：10 8 6 4 2
```

规则和正步长一样：**起点包含，终点不包含**——`range(5, 0, -1)` 包含 5，不包含 0。

**负步长的陷阱：起点必须大于终点**

```python
range(0, 5, -1)    # ❌ 空的！从 0 往 -1 走，永远到不了 5
range(5, 0, -1)    # ✅ 从 5 往 0 走，步长 -1
```

你不可能"向下走"到达一个比起点更大的数。如果步长是负的，起点必须 > 终点；步长是正的，起点必须 < 终点。

**完整用法速查：**

```python
range(5)           # 0, 1, 2, 3, 4
range(2, 6)        # 2, 3, 4, 5
range(0, 10, 3)    # 0, 3, 6, 9         — 正向跳跃
range(5, 0, -1)    # 5, 4, 3, 2, 1      — 倒计时
range(10, 0, -2)   # 10, 8, 6, 4, 2     — 反向跳跃
range(0, 5, -1)    # (空)               — 起点 < 终点 + 负步长 → 空
```

### 9.3 遍历列表

```python
fruits = ["苹果", "香蕉", "橘子"]
for fruit in fruits:
    print(f"我喜欢吃{fruit}")
# 迭代变量 fruit 依次取列表中的每个元素
```

### 9.4 累加器模式

```python
# 计算 1 到 100 的和
total = 0
for i in range(1, 101):
    total = total + i    # 也可以用 total += i
print(total)  # 5050
```

---

### 练习任务

**任务（⭐ 容易）**：用 for 循环打印 1 到 10 的乘法表。给定一个数字 `n`（比如 `n = 7`），输出：

```
7 × 1 = 7
7 × 2 = 14
...
7 × 10 = 70
```

**任务（⭐⭐ 中等）**：用 for 循环输出**9×9 乘法表**（阶梯形），格式如下：

```
1×1=1
2×1=2  2×2=4
3×1=3  3×2=6  3×3=9
...
9×1=9  9×2=18  ...  9×9=81
```

> 提示：外层循环 `for i in range(1, 10)`，内层循环 `for j in range(1, i+1)`，每行用 `print(..., end="	")`，行末用空 `print()` 换行。

**任务（⭐⭐⭐ 困难）**：输出一个"阶梯乘法表"，但只保留乘积为**偶数**的结果：

```
1×2=2
2×1=2  2×2=4  2×3=6  2×4=8
3×2=6  3×4=12
...
9×2=18  9×4=36  9×6=54  9×8=72
```

> 提示：在内层循环中用 `if (i * j) % 2 == 0` 判断乘积是否为偶数。综合运用：for 循环（第9章）+ 条件判断（第7章）+ `range()` 步长。

---

💡 **思考延续**

`for` 循环这个概念，比第一台电子计算机早了整整一个世纪。

1843 年，英国数学家 **Ada Lovelace** 在翻译一篇关于 Charles Babbage "分析机"（一种纯机械的、由齿轮和曲柄驱动的计算设备）的法语论文时，加入了大量自己的注释。在这些注释中，她写下了一段详细的算法——如何让分析机**反复执行同一组操作来计算伯努利数**。后世认为这就是世界上第一个计算机程序。

Ada 比所有人都早意识到一件事：机器不只可以算数字，它还可以**重复执行一组指令**——只要记录下上一轮的结果，用它作为下一轮的输入。这就是循环的核心思想。她还预见到这种机器可以用来处理音乐符号、文字和其他非数值信息，这在一百多年后才变成现实。

所以当你写 `for i in range(5):` 的时候，你踩在一条绵延 180 年的思考传统上。Ada Lovelace 在纸上写下那份笔记时，大概想象不到，一个多世纪后的初学者每天都可以在几毫秒内完成她当年只能靠想象的计算。
