# 第20章 PWM 呼吸灯——让 LED 像呼吸一样渐变

> **难度**：⭐⭐⭐  
> **所属教程**：Python 零基础入门到 MicroPython 嵌入式实战  
> **前置章节**：第19章（按键输入）

---

### 20.1 什么是 PWM

PWM（脉冲宽度调制）通过快速开关来模拟不同的电压输出。在一个很短的时间周期里，如果 50% 时间高电平、50% 时间低电平，LED 看起来就是 50% 亮度。

### 20.2 MicroPython 的 PWM

```python
from machine import Pin, PWM
import time

led = PWM(Pin(13), freq=5000)   # 频率 5000Hz

# duty 范围 0~1023（10 位分辨率）
led.duty(0)      # 最暗（灭）
led.duty(512)    # 约 50% 亮度
led.duty(1023)   # 最亮
```

### 20.3 呼吸灯效果

```python
from machine import Pin, PWM
import time

led = PWM(Pin(13), freq=5000)

while True:
    # 亮度逐渐增加
    for duty in range(0, 1024, 4):
        led.duty(duty)
        time.sleep(0.005)

    # 亮度逐渐减小
    for duty in range(1023, -1, -4):
        led.duty(duty)
        time.sleep(0.005)
```

LED 会像呼吸一样，从暗到亮，再从亮到暗，不断循环。

---

### 练习任务

### 练习任务

**任务（⭐ 容易）**：接一个 LED，让它做一个完整的呼吸周期（从暗到亮再到暗），速度是示例的两倍（提示：把 `time.sleep(0.005)` 改成 `0.0025`，或者步长从 4 改成 8）。

**任务（⭐⭐ 中等）**：用按键切换 3 种 LED 模式：①呼吸灯 ②常亮 ③闪烁。按下按键循环切换。

> 提示：定义一个变量 `mode = 0`，每次按下 `mode = (mode + 1) % 3`，然后用 `if/elif` 判断当前模式执行不同效果。

**任务（⭐⭐⭐ 困难）**：实现一个**LED 亮度控制器**：

1. 按一下按键，LED 亮度增加一档（共 5 档：0%、25%、50%、75%、100%）
2. 到最高档后再按，回到 0% 重新开始
3. 用另一个按键实现"呼吸灯/常亮"模式切换
4. 在 Thonny Shell 中实时打印当前亮度和模式

```
输出参考：
模式：呼吸灯 | 亮度：50%
模式：常亮 | 亮度：100%
```

> 综合运用：PWM（第19章）+ 按键输入（第18章）+ 模块化函数（第12章）+ 条件判断（第7章）。

---

💡 **思考延续**

PWM 是你已经每天都在用的技术。调节手机屏幕亮度时，屏幕背光 LED 并没有真正变暗——它只是以几千赫兹的频率在"亮"和"灭"之间快速切换。你觉得是"50% 亮度"，实际上是 50% 亮 + 50% 灭——你的眼睛来不及分辨，自动帮它们"平均"了。

同样的原理也用于：笔记本风扇调速、电饭煲温控、电动自行车的加减速。PWM 之所以普遍，是因为它只用开/关一个引脚（不需要真正的模拟信号），却能精确模拟"不多不少刚刚好"的效果。
